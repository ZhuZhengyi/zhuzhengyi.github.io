# SPDK编程框架

## 简介

## 源码

```c
void spdk_app_opts_init(struct spdk_app_opts *opts);

//启动spdk 应用框架
//改函数调用前，opts需要spdk_app_opts_init函数初始化。
//启动后，框架将以ctx为参数，在当前系统线程上调用start_fn；
//函数一直阻塞，直到spdk_app_stop被调用；
int spdk_app_start(struct spdk_app_opts* opts, spdk_msg_fn start_fn, void *ctx);
```

## 参考

1. 
