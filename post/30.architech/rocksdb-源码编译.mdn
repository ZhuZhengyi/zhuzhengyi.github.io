# Rocksdb源码编译

## 编译过程

rocksdb编译由`Makefile`控制。

Makefile -> `build_tools/build_detect_platform`

`build_detect_platform`: 检查编译平台及依赖库, 设置编译变量参数，将编译变量参数`make_config.mk`

```makefile
CFLAGS += ${EXTRA_CFLAGS}
CXXFLAGS += ${EXTRA_CXXFLAGS}
LDFLAGS += $(EXTRA_LDFLAGS)


LIBNAME=librocksdb
LIBRARY = ${LIBNAME}.a

static_lib: $(LIBRARY)
```
